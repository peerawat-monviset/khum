[package]
name = "khum"
version = "0.0.1"
edition = "2024"

[dependencies]
# Zero dependencies. Standard library only.

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
opt-level = 3
strip = true
