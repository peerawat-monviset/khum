[package]
name = "khum"
version = "0.0.1"
edition = "2024"

[dependencies]
tokio = { version = "1.38", features = ["rt-multi-thread", "net", "io-util", "time", "macros"] }

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
