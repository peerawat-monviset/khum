cargo-features = ["panic-immediate-abort"]

[package]
name = "khum"
version = "0.0.1"
edition = "2024"

[dependencies]
# Just std.

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "immediate-abort"
strip = true
